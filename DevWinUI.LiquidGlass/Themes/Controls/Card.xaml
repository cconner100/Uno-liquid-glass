<!-- Card: a Liquid Glass content card with optional icon, header and description. -->
<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:local="using:DevWinUI.LiquidGlass">

  <Style TargetType="local:Card" BasedOn="{StaticResource LiquidGlassDevCardStyle}" />

  <Style x:Key="LiquidGlassDevCardStyle" TargetType="local:Card">
    <Setter Property="Background" Value="{ThemeResource LiquidGlassFillBrush}" />
    <Setter Property="BorderBrush" Value="{ThemeResource LiquidGlassStrokeBrush}" />
    <Setter Property="BorderThickness" Value="1" />
    <Setter Property="CornerRadius" Value="{StaticResource LiquidGlassCardCornerRadius}" />
    <Setter Property="Padding" Value="16" />
    <Setter Property="Foreground" Value="{ThemeResource LiquidGlassLabelBrush}" />
    <Setter Property="FontSize" Value="15" />
    <Setter Property="HorizontalAlignment" Value="Stretch" />
    <Setter Property="HorizontalContentAlignment" Value="Stretch" />
    <Setter Property="VerticalContentAlignment" Value="Stretch" />
    <Setter Property="IsTabStop" Value="False" />
    <Setter Property="Template">
      <Setter.Value>
        <ControlTemplate TargetType="local:Card">
          <Grid x:Name="PART_Root"
                Background="{TemplateBinding Background}"
                BorderBrush="{TemplateBinding BorderBrush}"
                BorderThickness="{TemplateBinding BorderThickness}"
                CornerRadius="{TemplateBinding CornerRadius}"
                Padding="{TemplateBinding Padding}">
            <VisualStateManager.VisualStateGroups>
              <VisualStateGroup x:Name="CommonStates">
                <VisualState x:Name="Normal" />
                <VisualState x:Name="PointerOver">
                  <VisualState.Setters>
                    <Setter Target="PART_Root.Background" Value="{ThemeResource LiquidGlassFillPointerOverBrush}" />
                  </VisualState.Setters>
                </VisualState>
                <VisualState x:Name="Pressed">
                  <VisualState.Setters>
                    <Setter Target="PART_Root.Background" Value="{ThemeResource LiquidGlassFillPressedBrush}" />
                  </VisualState.Setters>
                </VisualState>
              </VisualStateGroup>
            </VisualStateManager.VisualStateGroups>
            <Grid.RowDefinitions>
              <RowDefinition Height="Auto" />
              <RowDefinition Height="*" />
            </Grid.RowDefinitions>
            <Grid x:Name="PART_HeaderGrid">
              <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="*" />
              </Grid.ColumnDefinitions>
              <ContentPresenter x:Name="PART_IconPresenter"
                                Margin="0,0,12,0"
                                VerticalAlignment="Center"
                                FontSize="20"
                                Content="{TemplateBinding Icon}" />
              <StackPanel Grid.Column="1"
                          VerticalAlignment="Center"
                          Spacing="2">
                <ContentPresenter x:Name="PART_HeaderPresenter"
                                  Content="{TemplateBinding Header}"
                                  FontSize="15"
                                  FontWeight="SemiBold"
                                  Foreground="{TemplateBinding Foreground}"
                                  TextWrapping="Wrap" />
                <ContentPresenter x:Name="PART_DescriptionPresenter"
                                  Content="{TemplateBinding Description}"
                                  FontSize="13"
                                  Foreground="{ThemeResource LiquidGlassSecondaryLabelBrush}"
                                  TextWrapping="Wrap" />
              </StackPanel>
            </Grid>
            <ContentPresenter x:Name="PART_ContentPresenter"
                              Grid.Row="1"
                              HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                              VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
                              Content="{TemplateBinding Content}"
                              ContentTemplate="{TemplateBinding ContentTemplate}" />
          </Grid>
        </ControlTemplate>
      </Setter.Value>
    </Setter>
  </Style>
</ResourceDictionary>
