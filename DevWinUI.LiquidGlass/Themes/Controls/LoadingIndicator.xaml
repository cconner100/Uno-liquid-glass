<!-- LoadingIndicator: animated loading control; per-mode visuals and storyboards are generated in code into PART_Root. -->
<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:local="using:DevWinUI.LiquidGlass">

  <Style TargetType="local:LoadingIndicator" BasedOn="{StaticResource LiquidGlassLoadingIndicatorStyle}" />

  <Style x:Key="LiquidGlassLoadingIndicatorStyle" TargetType="local:LoadingIndicator">
    <Setter Property="Foreground" Value="{ThemeResource LiquidGlassTintBrush}" />
    <Setter Property="MinWidth" Value="36" />
    <Setter Property="MinHeight" Value="36" />
    <Setter Property="HorizontalAlignment" Value="Center" />
    <Setter Property="VerticalAlignment" Value="Center" />
    <Setter Property="Template">
      <Setter.Value>
        <ControlTemplate TargetType="local:LoadingIndicator">
          <Grid x:Name="PART_Root" />
        </ControlTemplate>
      </Setter.Value>
    </Setter>
  </Style>
</ResourceDictionary>
